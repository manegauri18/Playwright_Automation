let str = "prog9ramming2";

let digits = "";
let nonDigits = "";

for (let ch of str) 
  {
    if (ch >= '0' && ch <= '9') 
    {
        digits = digits + ch;
    } 
    else 
    {
        nonDigits = nonDigits + ch;
    }
}

console.log(nonDigits + digits);