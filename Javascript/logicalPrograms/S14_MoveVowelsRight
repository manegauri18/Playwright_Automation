let str = "programming";

let vowels = "";
let nonVowels = "";

for (let ch of str) 
  {
    if (ch === 'a' || ch === 'e' || ch === 'i' || ch === 'o' || ch === 'u') 
      {
        vowels += ch;
    } 
    else 
    {
        nonVowels += ch;
    }
}

console.log(nonVowels + vowels);