let str = "XXHello Java";
let map = new Map();

for (let ch of str) 
{
    map.set(ch, (map.get(ch) || 0) + 1);
}

for (let [key, value] of map) 
  {
    if (value === 1) 
      {
        console.log(key);
        break;
    }
}