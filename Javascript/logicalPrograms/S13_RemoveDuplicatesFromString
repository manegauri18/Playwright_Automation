let input = "programming998";

let seen = new Set();
let unique = "";
let duplicate = "";

for (let ch of input) 
  {
    if (seen.has(ch)) 
      {
        duplicate += ch;
    } else 
      {
        seen.add(ch);
        unique += ch;
    }
}

console.log("Original:", input);
console.log("Without duplicates:", unique);
console.log("Duplicate:", duplicate);